{
  "name": "fx-accumulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "24.x"
  }
}